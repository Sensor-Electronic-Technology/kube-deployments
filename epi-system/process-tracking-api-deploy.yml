apiVersion: apps/v1
kind: Deployment
metadata:
  name: process-tracking-api
  namespace: epi-system
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: process-tracking-api
    spec:
      containers:
      - name: process-tracking-api
        image: aelmendorf234/process-tracking-api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_URLS
          value: http://*:8080
        - name: TZ
          value: America/New_York
  selector:
    matchLabels:
      app: process-tracking-api
---
apiVersion: v1
kind: Service
metadata:
  name: process-tracking-api
  namespace: epi-system
  annotations:
    metallb.universe.tf/allow-shared-ip: "key-to-share-1.2.3.4"
spec:
  loadBalancerIP: 172.20.4.208
  type: LoadBalancer
  ports:
  - port: 34000
    targetPort: 8080
  selector:
    app: process-tracking-api