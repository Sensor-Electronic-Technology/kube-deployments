apiVersion: apps/v1
kind: Deployment
metadata:
  name: category-api-dev
  namespace: epi-system
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: category-api-dev
    spec:
      containers:
      - name: category-api-dev
        image: aelmendorf234/category-management-api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_URLS
          value: http://*:8080
        - name: TZ
          value: America/New_York
  selector:
    matchLabels:
      app: category-api-dev
---
apiVersion: v1
kind: Service
metadata:
  name: category-api-dev
  namespace: epi-system
  annotations:
    metallb.universe.tf/allow-shared-ip: "key-to-share-1.2.3.4"
spec:
  loadBalancerIP: 172.20.4.208
  type: LoadBalancer
  ports:
  - port: 52000
    targetPort: 8080
  selector:
    app: category-api-dev